<% @users.each do |user| %>
  <%= link_to user.name, user_path(user.id) %> / <%= user.age %>  / <%= user.sex_i18n %> / <%= user.status_i18n %> / <%= user.birth_i18n %> / <%= render "follow_form", user:user %><br />
<% end %>
